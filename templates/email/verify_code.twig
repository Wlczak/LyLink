<!doctype html>
<html lang="en">

<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>{{ app_name|default('LyLink')|e }} — Verification Code</title>
  <style>
    body {
      margin: 0;
      padding: 0;
      background-color: #f5f7fb;
      font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
    }

    .container {
      width: 100%;
      max-width: 600px;
      margin: 32px auto;
      background: #ffffff;
      border-radius: 8px;
      overflow: hidden;
      box-shadow: 0 4px 18px rgba(0, 0, 0, 0.08);
    }

    .header {
      padding: 20px;
      text-align: left;
      background: #202020;
      color: #ffffff;
    }

    .header h1 {
      margin: 0;
      font-size: 18px;
      font-weight: 600;
    }

    .body {
      padding: 28px;
      color: #1f2937;
    }

    .greeting {
      margin: 0 0 12px 0;
      font-size: 16px;
    }

    .notice {
      margin: 0 0 18px 0;
      font-size: 14px;
      color: #4b5563;
    }

    .code-box {
      display: block;
      width: 100%;
      text-align: center;
      padding: 18px 12px;
      border-radius: 6px;
      background: #f3f4f6;
      border: 1px solid #e5e7eb;
      font-size: 28px;
      letter-spacing: 6px;
      font-weight: 700;
      font-family: "Courier New", Courier, monospace;
      color: #111827;
      box-sizing: border-box;
    }

    .muted {
      margin-top: 16px;
      font-size: 12px;
      color: #6b7280;
    }

    .footer {
      padding: 16px 20px;
      font-size: 12px;
      color: #9ca3af;
      text-align: center;
      background: #fafafa;
    }

    @media (max-width:480px) {
      .code-box {
        font-size: 22px;
        letter-spacing: 4px;
        padding: 14px 10px;
      }

      .body {
        padding: 18px;
      }
    }
  </style>
</head>

<body>
  <table role="presentation" width="100%" cellpadding="0" cellspacing="0" style="background-color:transparent;">
    <tr>
      <td align="center">
        <table role="presentation" class="container" cellpadding="0" cellspacing="0">
          <tr>
            <td class="header">
              <h1>{{ app_name|default('LyLink')|e }}</h1>
            </td>
          </tr>
          <tr>
            <td class="body">
              <p class="greeting">Hi {{ username|default('')|e }},</p>
              <p class="notice">Use the verification code below to confirm your email address for your
                {{ app_name|default('LyLink')|e }} account. Enter this code in the verification form. Do not share it
                with anyone.</p>
              <div class="code-box" aria-label="Verification code">{{ code|e }}</div>
              <p class="muted">This code will expire in {{ expires_minutes|default(30) }} minutes.</p>
            </td>
          </tr>
          <tr>
            <td class="footer">If you did not request this, you can ignore this email.</td>
          </tr>
        </table>
      </td>
    </tr>
  </table>
</body>

</html>